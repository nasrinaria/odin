## Notifier Service
Listens to events (e.g. user.created, review.completed) via pub/sub.
Sends user notifications (welcome email, daily reminders, push notifications later).
Runs asynchronously and doesn’t expose a public API.
