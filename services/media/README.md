## Media Service
Manages file uploads (images for boxes and cards).
Generates presigned URLs so clients upload directly to object storage (S3/DO Spaces).
Stores only file metadata/URLs in the database.
Can later handle thumbnails or file validation.
